apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool

metadata:
  name: pool
  namespace: kafka
  labels:
    strimzi.io/cluster: lab-cluster

spec:
  replicas: 1

  roles:
    - controller
    - broker

  storage:
    type: ephemeral